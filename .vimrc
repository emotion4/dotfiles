" this shits so retarded its actually impossible to maintain
" everyday i find new (not neccessarily) bugs and spend a trillion years trying to fix them
" shit contains so many fucking awful hacks and fixes
"
" note: instead of using bdelete, bwipeout, use BbyeDelete, BbyeWipeout
"                        q to quit windows, use Wdelete
"
" using keybinds whenever possible is preferred and recommended
" please use actively maintained plugins if you still have the will to live

if filereadable(expand('~/.vim/plug.vim'))
    source ~/.vim/plug.vim
end

if filereadable(expand('~/.vim/config.vim'))
    source ~/.vim/config.vim
end
